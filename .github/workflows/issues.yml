name: Project Issue Management
on: issues
jobs:
  output-issue-info:
    runs-on: unbuntu-latest
    steps:
      - name: Handle Issue Events
        run: |
          echo "Issue: ${{toJson(github)}}"
          echo "This workflow is triggered manually to manage issues in the project."
